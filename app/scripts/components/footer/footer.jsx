var React = require('react');
var Footer = React.createClass({
      render: function(){
        return (
          <footer>
            <p>This is a footer in jsx</p>
          </footer>
        )
      }
    });

module.exports = Footer;
